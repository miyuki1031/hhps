import { CONTENTS } from "../../lib/constants";

export default async function Profile() {
    const cotentId: string = "PROFILE";

    return (
        <main className="p-10 flex flex-col items-center justify-center min-h-screen">
            <h1 className="text-3xl font-bold text-blue-600">プロフィル</h1>
        </main>
    );
}
